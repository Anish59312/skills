// Commands specific to this app. Frappe's commands are imported separately in e2e.js — check
// references/cypress-commands.md before adding one here, most of what you need already exists.

// Example: a fixture helper backed by an @whitelist_for_tests endpoint in
// <app>/tests/ui_test_helpers.py.
//
// Cypress.Commands.add("setup_ui_test_data", () => {
// 	return cy.call("myapp.tests.ui_test_helpers.setup_ui_test_data");
// });

// Example: assert a field's value without depending on the input's markup.
//
// Cypress.Commands.add("assert_field_value", (fieldname, expected) => {
// 	cy.window()
// 		.its("cur_frm.doc")
// 		.then((doc) => expect(doc[fieldname]).to.equal(expected));
// });
